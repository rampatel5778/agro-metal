---
/**
 * ServiceCardGrid — services hub cards
 * @example
 * <ServiceCardGrid items={services} columns={3} />
 */
import type { ServiceCardGridProps } from '@types/components/business.js';
import { cn } from '@utils/classNames.js';
import Card from '@components/ui/Card.astro';
import Image from '@components/ui/Image.astro';
import Button from '@components/ui/Button.astro';

const {
  items,
  columns = 3,
  class: className = '',
  ariaLabel = 'Services',
} = Astro.props as ServiceCardGridProps;

const colClass = `ds-grid--${columns}` as const;
---

<ul class={cn('ds-grid', colClass, className)} aria-label={ariaLabel}>
  {
    items.map((item) => (
      <li>
        <Card variant="service" href={item.href} class="ds-card--lift h-full">
          {
            item.image && (
              <div class="ds-card__media">
                <Image
                  src={item.image.src}
                  alt={item.image.alt}
                  width={item.image.width}
                  height={item.image.height}
                  aspect="portrait"
                />
              </div>
            )
          }
          <div class="ds-card__body">
            <h3 class="ds-card__title">{item.title}</h3>
            {item.description && <p class="ds-card__text">{item.description}</p>}
            {
              item.href && (
                <Button variant="link" href={item.href}>
                  Read more
                </Button>
              )
            }
          </div>
        </Card>
      </li>
    ))
  }
</ul>
